# 项目级 AI 指令 — 后端环境

> 本文件是 AGENTS.md 的兼容入口，内容保持一致。
> 详见 [AGENTS.md](./AGENTS.md)

## 通用约束

- 所有改动遵循 **Less is More**：只留必要、最小改动、配置回归
- 不改动与任务无关的文件
- 移除时连带清理依赖、配置、引用，不留孤儿
- 不擅自 commit / push

## 语言规则

- 全程简体中文；代码、命令、路径、API、原始报错保持原文
- 面向用户的文档/教程用简体中文

## 安全底线

- 数据库仅限只读查询
- 不执行未经确认的写入操作

## 技能引用

本目录下 `skills/` 中的技能文件覆盖后端相关能力，按需触发。